# Robot H5 — AI 编码规范

> 本文件与 `.github/copilot-instructions.md` 内容同源，确保所有 AI 编辑器遵循同一规范。

## 项目概述

Vue 3.5 + Vite 7 + TypeScript 移动端 H5 应用，设计语言为 Apple HIG Liquid Glass。
UI 库 Vant 4（自动导入），原子 CSS 为 UnoCSS，状态管理 Pinia。

## 核心规范

### 三文件分离

每个页面/模块为独立目录：
- `index.vue` — 模板 + 逻辑，`<template>` 在上，`<script setup lang="ts">` 在下，**禁止 `<style>` 块**
- `index.scss` — 样式，BEM 命名，使用 `--ds-*` 设计令牌，通过 `import './index.scss'` 引入
- `data.ts` — 类型定义 + 常量映射 + 静态数据 + Mock 数据

### 组件命名

- 全局组件：`src/components/C_{Name}/`，自动注册
- 类型定义：`types/{Name}/type.ts`，使用 `#/{Name}/type` 别名导入
- 禁止在 `<script setup>` 中 `export` 类型

### 类型检查（零容忍）

- 每次修改后运行 `pnpm type-check`（vue-tsc --noEmit），必须零错误
- Vant 组件属性须符合类型约束

### 样式规范

- 禁止硬编码颜色/圆角/阴影 → 必须用 `var(--ds-xxx)` 令牌
- BEM 命名：`.{page-name}__{element}--{modifier}`
- 间距 4px 网格：4 / 8 / 12 / 16 / 20 / 24 / 32
- 字号梯度：11 / 12 / 13 / 14 / 15 / 16 / 17 / 20 / 22 / 28 / 34

### 图标规范

- UnoCSS 类名：`i-ph:{name}-bold`（Phosphor）或 `i-ic:{name}`（IC）
- 禁止 CDN 图标

### 路由规范

- 路由 `name` 必须与 `defineOptions({ name })` 完全一致
- 子页面注册在 `src/router/modules.ts`，TabBar 主页在 `src/router/menu.ts`

### 接口规范

- 一个业务模块一个文件：`src/api/{module}.ts`（扁平化）
- 使用 `import { get, post, put, del, toast } from '@/utils/http'`
- 命名：`get{Module}List` / `get{Module}Detail` / `add{Module}` / `update{Module}` / `delete{Module}`
- 每个 API 必须有对应 Mock（`mock/{module}.ts`，使用 vite-plugin-mock）

---

## 代码交付前自动审计（每次修改后必须执行）

**每次生成或修改 `.vue` / `.scss` / `data.ts` / `api/*.ts` 后，在交付给用户前自动执行以下审计并直接修复，无需用户触发。**

### 🔴 P0 — 静默修复

**样式令牌合规：**
- `.scss` 中 `#xxx` / `rgb()` / `rgba()` → `var(--ds-*)`
- `border-radius: Npx` → `var(--ds-radius-*)`
- `box-shadow` 字面量 → `var(--ds-shadow-*)`
- `.vue` 有 `<style` → 提取到 `.scss`
- `<script setup>` 无 `defineOptions` → 添加，name 与路由一致
- `<script setup>` 中 `export type/interface` → 移到 `types/`

**运行时安全（页面不可报错/白屏）：**
- `pnpm type-check` 必须零错误 → 修复所有类型错误
- 导入的组件/模块/函数必须存在且路径正确 → 修正路径
- 模板中引用的变量/方法必须在 `<script setup>` 中定义 → 补充定义
- 组件必填 props 必须传值 → 补充 prop
- 模板中不出现 `.value` → 修正响应式写法
- `router.push` 目标路由必须已注册 → 补充路由

### 🟡 P1 — 修复 + 报告

**样式规范：**
- 页面必须有 `<C_NavBar>` 开头
- 底部固定栏必须有 `env(safe-area-inset-bottom)`
- 间距必须是 4px 网格，字号必须在梯度范围内
- 状态字段必须有 `xxxMap` 常量 + `<VanTag>` 渲染

**原型/详设精细度对比（有原型或详设输入时执行）：**
- `data.ts` interface 字段 ≥ 原型字段，不可遗漏
- 按钮文案必须与原型一字不差
- 原型有的按钮不可少，没有的不可自编
- 区块顺序与原型一致
- 所有状态枚举值都有 Map 映射
- 不同状态不同按钮时用 `show: (row) => boolean` 控制
- 列表→详情→表单跳转路径与原型一致
- 每个 API 都有对应 Mock 端点

### 执行流程

```
生成/修改代码 → P0 静默修复 → P1 修复并报告 → pnpm type-check → 交付
```

---

## 设计令牌速查

**颜色：** `--ds-bg` `--ds-bg-secondary` `--ds-bg-tertiary` `--ds-surface` `--ds-text-primary` `--ds-text-secondary` `--ds-text-tertiary` `--ds-accent` `--ds-success` `--ds-warning` `--ds-danger` `--ds-border` `--ds-divider`

**圆角：** `--ds-radius-sm(8)` `--ds-radius-md(12)` `--ds-radius-lg(16)` `--ds-radius-xl(20)` `--ds-radius-full(9999)`

**阴影：** `--ds-shadow-1` `--ds-shadow-2` `--ds-shadow-3`

**毛玻璃：** `--ds-glass-bg` `--ds-glass-border` `--ds-glass-blur` `--ds-glass-saturate`

**间距（4px 网格）：** 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64

**字号：** 11 / 12 / 13 / 14 / 15 / 16 / 17 / 20 / 22 / 28 / 34

---

## Skills 自动调度

当用户提到以下关键词时，**必须先读取对应 Skill 文件再执行**：

| 触发词 | Skill | 文件路径 |
|--------|-------|---------|
| `扫描原型` / `分析原型` | prototype-scan | `.github/skills/prototype-scan/skills.md` |
| `生成接口规格` / `接口字段说明` | api-spec | `.github/skills/api-spec/skills.md` |
| `生成接口` / `接口约定` | api-contract | `.github/skills/api-contract/skills.md` |
| `生成页面` / `生成代码` | page-codegen | `.github/skills/page-codegen/skills.md` |
| `注册路由` / `添加菜单` | route-register | `.github/skills/route-register/skills.md` |
| `生成 Mock` / `补充模拟数据` | mock-gen | `.github/skills/mock-gen/skills.md` |
| `审计规范` / `代码检查` | convention-audit | `.github/skills/convention-audit/skills.md` |

**完整流水线**：用户提供原型并要求批量生成时，按 ①→⑦ 顺序依次执行。
**单独使用**：用户只请求特定操作时，只读取对应 Skill。
**convention-audit 自动执行**：每次代码变更后自动运行，无需触发。

---

## 详细参考

- 完整审计规则：`.github/skills/convention-audit/skills.md`
- 令牌映射表：`.github/prompts/convention-audit.prompt.md`
- 组件用法：各 `src/components/C_*/README.md`
- API 规范：`src/api/README.md`
- 设计系统：`DESIGN_SYSTEM.md`
- Skills 工作流：`docs/mobile-skills-proposal.md`
