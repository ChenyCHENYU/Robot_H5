# C_VirtualStatusBar — 虚拟状态栏

> 桌面端预览时模拟手机顶部状态栏（时间、信号、电池），移动端自动隐藏。

## 基本用法

已在 `App.vue` 中全局引入，无需手动使用：

```vue
<C_VirtualStatusBar />
```

## 行为

- **移动端**（< 576px）：`display: none` 不渲染
- **桌面端**（≥ 576px）：显示模拟状态栏，高度 30px
- 显示内容：当前时间 | 信号格 | 电池电量
